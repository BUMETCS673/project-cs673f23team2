// Get the reward points and name elements
const rewardPointsElement = document.querySelector('#reward-points');
const nameElement = document.querySelector('#name');

// Update the reward points and name elements
rewardPointsElement.textContent = 100;
nameElement.textContent = 'Paridhi Talwar';

